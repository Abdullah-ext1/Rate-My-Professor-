const PhoneFrame = ({ children }) => (
  <div className="w-96 bg-bg rounded-[40px] border-2 border-opacity-10 border-white overflow-hidden flex flex-col shadow-2xl" style={{ height: '812px' }}>
    {children}
  </div>
);

export default PhoneFrame;
